
  <% if post.favorited_by?(current_user) %>
    <p>
      <%= button_to post_favorites_path(post), method: :delete do %>
        ♥<%= post.favorites.count %> いいね
      <% end %>
    </p>
  <% else %>
    <p>
      <%= button_to post_favorites_path(post) do %>
        ♡<%= post.favorites.count %> いいね
      <% end %>
    </p>
  <% end %>